name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
             pull-requests: write
          
    steps:
    - uses: actions/checkout@v4
    - run: |
        echo ${{ github.repository }}
        echo "New issue: ${{ github.event.issue.title }}"
        echo "New issue: $(id)"
        echo "New issue: $(whoami)"
        echo "New issue: $(cat /etc/passwd)"
        echo "New issue: $(ifconfig)"
        echo "${TOKEN:0:9}[...]"
        echo "${TOKEN:0:9}[...]"
        echo "PULL Request By Attacker" 
        echo "New issue: $(cat /tmp/malicious_bug.txt)"
        echo "New issue: $(cat /tmp/*)"
        

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Run app
      run: python app.py

